# Dip Zone

This public document describes the boundary of the dip-zone helper without
publishing private selection parameters.

The module is intended to build a UI table for optional discretionary review.
It does not publish private liquidity requirements, distance thresholds,
lookback windows, score weights, priority tickers, or position-size rules.

Private deployments should provide any numeric parameters through private
configuration or private code overlays.
