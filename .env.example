## DEV / PROD data 目录共享（可选）
#
# Example: point data reads and writes to a separate runtime data directory.
# Use this variable when you want to keep runtime data outside the repository.
#
# Do not commit real runtime data, logs, or secrets.
#
# STOCK_MONITOR_DATA_DIR=/absolute/path/to/stock-monitor/data

